version: "2"

linters:
  default: standard
  enable:
    - misspell
    - gocritic
    - revive
    - unconvert
    - prealloc
    - copyloopvar
  exclusions:
    rules:
      - path: "_test\\.go"
        linters:
          - gocritic
          - prealloc

formatters:
  enable:
    - gofmt
